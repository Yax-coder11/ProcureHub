/**
 * DEPRECATED — use src/routes/ProtectedRoute.jsx instead.
 *
 * This file re-exports the new ProtectedRoute so existing import paths
 * in App.jsx continue to work without changes.
 */
export { default } from '../routes/ProtectedRoute'
