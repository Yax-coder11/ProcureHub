/**
 * DEPRECATED — do not import this file in new code.
 *
 * All API calls must use:  import apiClient from '../api/apiClient'
 *
 * This file re-exports apiClient so legacy imports don't break
 * while the codebase is being migrated.
 */
export { default } from '../api/apiClient'
